node_modules/
dist/
*.log
.DS_Store
.env
.env.local

# Capacitor copies the web build here; it's regenerated by `cap sync`.
ios/App/App/public/

# NOTE: the generated `ios/` project itself is intentionally NOT ignored —
# Codemagic builds from the committed iOS folder (see SETUP.md).
